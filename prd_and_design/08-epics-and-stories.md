# Epic/Story 拆分与进度

## 文档目的

本文档是项目的 SDD 交付台账，记录项目阶段、Epic/Story 范围、当前状态和人工验收门禁。产品语义仍以 PRD 为准，实现方式仍以技术设计为准。

## 状态定义

- 项目进度：需求澄清、原型设计、技术方案设计、代码开发、交付完成。
- Epic 进度：待启动、开发中（必须注明当前 Story）、开发完成。
- Story 进度：待启动、开发中、集成测试中（必须注明当前用例）。
- “代码已存在”不等于“Story 已完成”；单元测试、集成测试和用户人工验收都通过后，才能结束 Story。

## 项目总进度

| 项目 | 当前值 |
| --- | --- |
| 当前阶段 | 代码开发 |
| 当前 Epic | E1 Hook 采集与 Trace 可视化 |
| 当前 Story | S2 transcript 内容补齐 |
| 当前测试 | 集成测试中：`IT-HOOK-TRACE-002` 步骤 1–10 已通过；步骤 11 补扫零新增、零失败，待页面确认无重复 |
| 下一门禁 | 完成步骤 11 页面确认及步骤 12 代码阅读并明确接受后，S2 才能结束 |

## Epic 与 Story

### E0 规格、设计与领域架构基线

- 状态：开发完成。
- 产出：PRD、V1/V2 原型、技术设计、数据关联模型、MySQL 切换方案、领域化架构升级规格。
- 注意：本次补写的 SDD 台账不改变已冻结的 V1/V2 产品与交互语义。

| Story | 范围 | 状态 | 当前证据/门禁 |
| --- | --- | --- | --- |
| S0 领域化架构升级 | 按问题子域拆分 DDD 分层；Hook 主链路结构化 DTO、Raw Event 特例、调度器/应用服务/领域模型/持久化边界 | 开发完成 | 用户已完成代码阅读；自动回归与 Story1 步骤 1–10 人工回归均通过 |

### E1 Hook 采集与 Trace 可视化

- 状态：开发中，当前到 S2。
- 目标：用 Hook 建立 Session/Turn/Tool 行为骨架，用 transcript 补内容，用 OTel 补性能，并在正式前端绘制 Trace。

| Story | 范围 | 状态 | 当前证据/门禁 |
| --- | --- | --- | --- |
| S1 真实 Codex Hook 主链路 | Python 3.11 运行环境、Hook 配置与信任、forwarder、Hook 接收、MySQL 骨架、列表与 Trace 行为节点 | 开发完成 | `IT-HOOK-TRACE-001` 步骤 1–10 已在 S0 后人工回归通过；用户于 2026-09-16 明确接受 Story |
| S1.1 本地运行日志 | CLI 与后端结构化按日日志、7 天保留、敏感字段脱敏；不含前端日志 | 开发完成 | CLI 6 项、后端 JDK 17/MySQL 全量 36 项及 `IT-LOG-001` 人工验收通过；用户于 2026-09-16 完成代码阅读并明确接受 Story |
| S2 transcript 内容补齐 | 路径安全、session_meta 校验、用户/工具/最终内容挂接与三源证据检查 | 集成测试中（`IT-HOOK-TRACE-002` 步骤 11） | 后端 JDK 17/MySQL 全量 49 项、前端 19 项及生产构建通过；步骤 1–10 已通过，步骤 11 待页面确认，之后进行代码阅读 |
| S3 OTel 性能 Trace | OTLP traces 接收、工具精确关联、耗时摘要守恒、精度标识 | 待启动 | S2 验收后开始；TTFT/API 不得用 Hook 伪造 |
| S4 实时 Trace | SSE 更新、运行中到完成、缺失内容标记 | 待启动 | S3 验收后开始 |
| S5 健壮性与降级 | 幂等、乱序、半行、错配、UNKNOWN、缺源、同名并行歧义 | 待启动 | S4 验收后开始 |

| 强关联后续 Story | 所属范围 | 状态 | 说明与启动条件 |
| --- | --- | --- | --- |
| S3.1 Java 服务层 OTel 精确关联重构 | E1-S3 OTel 性能 Trace | 待启动（后续优先评估；强关联 S1） | 当前实现将 `Hook.tool_use_id == OTel codex.call_id` 直接写成 `EXACT`，但该相等性未由真实 Codex 样本验证，且不得视为默认跨命名空间契约。实施时将候选筛选、已验证公共 ID 判定、证据构建、算法版本和时间差计算迁移到 Java 服务层；Mapper 仅保留候选读取、关联写入和查询。S3 启动前先更新技术设计，补充真实样本契约、乱序、重复、空 ID 与不一致 ID 测试后再实施 |

### E2 完整性、指标与诊断

- 状态：待启动。
- 范围：完整的 API/TTFT/审批/本地处理归因、异常耗时贡献、确定性诊断与全量统计口径。
- 启动条件：E1 完成。当前实现中 `modelRequestMs`、`approvalMs` 和 `localMs` 仍不代表 PRD 要求的完整能力。

| 长期 Story | 范围 | 状态 | 说明 |
| --- | --- | --- | --- |
| E2-S1 运行指标持久化与高可用聚合 | 替换 Hook 接收服务中进程内计数器与样本列表；设计跨重启保留、多实例聚合、窗口/分位数计算、容量上限、降级与一致性语义 | 待启动（长期） | 当前 `requests`、`accepted`、`duplicates`、错误计数、`lastSuccessAt` 与 `processingSamples` 仅适用于单进程运行期，状态接口不得被解释为高可用或长期统计。开始前必须完成独立技术方案、数据模型和故障/并发测试 |

### E3 交付、安装与运维

- 状态：待启动。
- 范围：可复现打包、Hook 安装/检测、日志轮转、敏感信息扫描、完整交付验收。

## 进度更新规则

1. 每次开始 Story 前，先更新本文档和对应 Story 规格。
2. 开发中同步写单元测试；单元测试通过后，Story 才进入集成测试中。
3. 集成测试记录必须分开“自动执行”和“用户人工验收”，不得互相替代。
4. 未经用户明确同意，不得越过当前 Story 的人工门禁。
5. 暂缓 Story 仅登记优先级与边界；未补齐独立规格、测试用例并获用户启动指令前，不得实施。
6. 架构升级 Story 完成后，必须先回归受影响的已验收 Story，再解除后续集成测试门禁。
