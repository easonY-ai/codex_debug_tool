# 仓库协作规则

## 必须读取的项目上下文

在规划或实施任何修改前，必须完整阅读 `prd_and_design/README.md`，并继续完整阅读其中“文档索引”引用的所有文档。将这些文件视为项目的持久上下文，不得依赖历史对话补充缺失的需求。

处理实现问题时，按以下优先级参考：

1. `prd_and_design/01-product-requirements.md` 定义产品语义、指标口径和数据边界。
2. `prd_and_design/prototype-v2` 定义当前已确认的 Hook-first 页面结构、视觉层级和交互方式；V1 仅用于追溯旧基线。
3. `prd_and_design/03-data-and-correlation.md` 定义 Hook、JSONL、OTel 和跨数据源关联规则。
4. `prd_and_design/02-technical-design.md` 定义工程实现方式。

如果这些来源互相冲突，或者无法确定一项会实质影响产品的决策，不得自行选择或静默假设。必须说明冲突，并先更新需求，再开始实现。

进入正式编码前必须执行需求、技术方案、原型和当前实现的一致性门禁：所有阻塞问题必须先解决；其他强烈建议解决的问题必须逐项明确提出并取得用户确认后，方可继续编码。

## 编译与运行环境要求

- java使用jdk17
- python使用3.11。使用uv做项目管理，项目级别的运行环境
- 数据库使用mysql

## 敏捷开发要求

- 采用敏捷开发模式，不要一次性开发所有功能，将功能拆解为epic/story，按顺序开发，阶段性验收
- 在第一个story中需要完成环境的搭建和验收工作。可以独立为一个story，也可以和第一个需求story合并，视story的复杂性而定

## 受保护的设计基线

- `prd_and_design/prototype-v1` 是已冻结、可复现的设计基线。不得修改、格式化或重新生成该目录中的任何文件。
- `prd_and_design/prototype-v2` 是当前已确认并冻结的设计基线。进入正式编码后不得修改、格式化或重新生成；新变化必须先更新 PRD 并创建 V3。
- 产品或交互发生变化时，必须先更新 PRD 和原型规格。需要新的可运行原型时，创建 `prd_and_design/prototype-v2` 等新版本，禁止覆盖 V1。
- 正式前端功能只能在 `frontend` 中实现。
- `backend` 目录及其里程碑建立后，后端功能只能在 `backend` 中实现。

## 事务与数据库逻辑

- 事务边界、提交与回滚统一由 Spring 的 `@Transactional` 或 `TransactionTemplate` 管理；业务流程、校验和重试优先在 Java 服务层实现。
- 尽量不使用 MySQL 触发器、存储过程、存储函数或数据库定时事件承载业务逻辑与事务编排。确有必要时，必须先在技术设计中说明 Java/Spring 方案不足、数据库权限要求及维护成本。
- 保留数据库事务、主外键、唯一约束和必要索引，作为持久化原子性、完整性与并发幂等的保障；不能仅用 Java 校验替代数据库约束。
- 回滚测试优先在 Java 测试边界注入异常，通过真实测试数据库验证写入和检查点一同回滚；不得为故障注入要求 MySQL 全局高权限或修改全局配置。

## 测试要求

- 前端和后端代码均必须编写并维护相应的单元测试。
- 单元测试必须能够在不依赖外部环境的情况下运行，不得要求网络、外部服务、真实 Codex 数据、本机专属配置或其他未由测试自身提供的运行条件。
- 后端接口可以在实施过程中独立测试，但这种阶段性测试不代表产品主流程或全部代码已经完成。
- 执行浏览器 E2E 测试前，必须先完成当前需求范围内的全部实现代码，并确认各交付面已经真实连通；不得使用 Mock 数据、占位实现或仅验证孤立页面交互来替代正式端到端链路。
- 当用户要求“完成所有代码”“实现完整代码”或同等含义的任务时，“所有代码”是指当前需求所需的全部对应实现，包括但不限于前端、后端，以及安装器、forwarder、Python CLI 或其他必要的命令行程序与脚本。不得把其中任一部分静默排除在完成范围之外。
- 宣布 E2E 或全部代码完成前，必须验证真实主流程能够跨越所有相关组件；若仍有未实现模块、Mock 数据源、未接通接口或缺失的 CLI/脚本，必须明确报告为未完成，不得缩小验收口径后宣称完成。

## 文档同步与仓库安全

- 已确认的需求、指标口径、架构、数据结构或关联规则发生变化时，必须同步更新 `prd_and_design`。
- 本项目是公开仓库。禁止提交真实账号信息、凭据、Token、私钥、本机专属路径、真实 Codex 对话或其他敏感数据。
- 示例、测试数据和截图必须使用明确的虚构内容，例如 `/workspace/demo-project` 和固定测试 ID。
- 禁止提交依赖目录、构建产物、本地数据库、JSONL 采集文件、日志或本地配置。
