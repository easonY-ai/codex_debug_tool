# 仓库协作规则

## 软件生命周期管理
- 采用specification driven development（SDD）开发模式。
    - 按 需求澄清、原型设计、技术方案设计、敏捷epic/story拆分、story开发、单元测试、集成测试 的方式推动项目交付。
    - 需要有明确的规格说明文档，记录项目推进的过程产出spec（需求文档、原型设计文档、技术方案设计文档、epic/story拆分文档、集成测试用例等）、流程和进度，包括每个story开发进度、每个epic的开发进度
        - 项目进度是：需求澄清、原型设计、技术方案设计、代码开发、交付完成
        - 代码开发进度摆阔 epic进展、story进展
            - epic进展：待启动、开发中、开发完成。开发中需要记录到开发到第几个story。
            - story进展：待启动、开发中、集成测试中（包括y公里的测试情况）。开发中，代码开发和单测迭代前进，不区分状态。单测完成后进入集成测试，集成测试需要记录测试到那个用例。
    - 需求澄清阶段必须先完成可行性验证，再进入细化产品方案设计。
        - 必须识别会影响产品范围、数据语义、核心体验或交付路径的关键技术卡点，并通过官方契约核对、最小化实验、真实样本采集或技术原型验证其可行性；规格中必须记录验证问题、方法、通过标准、证据、结论和剩余风险。
        - 可行性验证及其人工门禁未通过前，项目必须保持在“需求澄清”阶段，不得开始详细信息架构、交互原型、完整产品方案或正式代码实现；验证用的最小实验不视为正式实现。
    - 关键技术要点存在多种可行方案时，必须完成技术方案对比并明确选型逻辑。
        - 对比至少覆盖能力与需求匹配度、契约稳定性、实现与关联复杂度、运维成本、性能、安全与隐私、测试性、迁移和回滚风险；必须记录候选方案、采用方案、淘汰原因、适用前提和未决风险。
        - 方案选择必须由验证证据和产品目标共同支持，并取得用户确认；不得仅因已有代码较多、实现更熟悉或短期改动更少而静默选型。
    - 所有项目迭代，需要先修改必要的过程产出再修改代码，并完成对应的测试工作。bug fix中需要区分是实现错误还是需求或者技术方案升级，如果是实现错误，过程产出spec不用迭代，否则需要先迭代文档。
- 项目恢复与任务续接
    - 当重新恢复项目时，如果用户询问“当前的工作是什么”或同等问题，必须以当前 spec 为依据，检查项目进展、正在进行的 Epic/Story、当前测试用例与人工门禁，再告知用户下一个任务。如果有多个任务处于进行中，必须明确提醒用户优先续接哪一个任务。
    - 当用户指定某一个 Story 或某一项具体工作时，必须先提醒用户上一次记录的项目进展、当前任务及未完成门禁。如果用户指定的工作与当前任务不同，必须先确认是否切换到新任务，不得静默切换或越过当前 Story 的验收门禁。
- 敏捷开发要求
    - 除非明确要求实现所有story，否则不要一次性开发所有功能，按顺序开发story，每个story开发完成让人工验收，并确定是否进行下一个story。
    - 在第一个story中需要完成环境的搭建和验收工作。可以独立为一个story，也可以和第一个需求story合并，视story的复杂性而定。
- 采用测试驱动开发范式。
    - 先定接口、写用例，然后再开始开发。在用例编写过程中，反思接口定义是否合理。
    - 所有代码单元必须经过单元测试，才算交付。

## DDD 领域化架构规范

### 总体方案设计门禁

- 总体方案设计必须先完成问题域划分，再设计技术组件、数据库表或处理流程。问题域划分至少要明确核心域、支撑域、各域的业务能力、统一语言、限界上下文、上下文之间的协作契约和数据所有权；不得以输入来源、处理步骤、框架分层或现有目录直接代替问题域。
- 总体方案设计必须明确关键实体。每个关键实体至少要说明业务身份、生命周期、状态变化、业务不变量、所属问题域、是否为聚合根，以及用户或外部事件通过哪个聚合根发起操作；不能只列数据库表或 Java 类名。
- 正式编码前必须形成可评审的问题域清单、统一语言、上下文关系、关键实体与聚合决策表。若这些内容仍存在会影响业务语义、事务或数据所有权的歧义，只能继续更新规格并请求决策，不得进入代码实现。

### 聚合设计原则

- 聚合边界采用小聚合优先原则。只有必须在同一事务内始终保持一致的业务不变量，才允许扩大聚合；不得为了对象导航、页面查询、少写 Repository 或复用数据库关联而创建大聚合。
- 用户操作和外部业务命令必须从聚合根进入。设计聚合时必须从实际操作入口反推根：如果用户主要操作 Turn，就不能仅因 Session 包含多个 Turn 而要求每次通过 Session 聚合加载和修改 Turn。
- 决定聚合边界时必须评估最坏数据加载量、事务锁范围、并发冲突、生命周期和容量增长。父子关系、外键关系或页面同时展示，不自动构成同一聚合的理由。
- 查询模型与写模型分离。跨聚合或跨问题域的页面查询使用 `Query`、`Lookup`、`ReadModel` 等明确端口组合数据，不得为了查询便利扩大写聚合；查询端口不得命名为 Repository。
- 每个写模型聚合根只对应一个语义明确的 Repository。Application 负责编排多个聚合或问题域；Domain 只维护单个聚合内部的不变量。允许最终一致的跨聚合规则不得强行塞入单个大聚合。

### 分层与实现规则

- 后端采用按问题子域拆解的 DDD 结构；`interfaces`、`application`、`domain`、`infrastructure` 是依赖边界，不是按 `entity`、`service`、`enum`、`mapper` 等技术类型集中分目录。
- 每个分层内部继续按 `hook-ingestion`、`hook-normalization`、`trace-query` 等问题子域组织，目录和类型名称优先使用业务语言。
- 所有类型、方法、字段、参数和目录命名都必须优先表达业务含义、业务边界和职责；不得因技术便利使用含义模糊的名称。
- `interfaces` 只负责 HTTP、定时调度等入站适配；`application` 负责用例编排、事务和端口调用；`domain` 负责聚合、实体、值对象和领域规则；`infrastructure` 负责框架、序列化和持久化适配。
- 领域服务用于封装重要且可复用的业务规则与状态演进。例如 Hook 事件类型如何推进 Session、Turn、Tool 生命周期，必须由对应领域服务和聚合模型表达；领域服务不处理 HTTP、框架或具体持久化实现。
- 应用服务用于实现具体用户用例：定义事务边界、调用并编排多个领域服务及端口、处理用例级错误和结果；不得把应由单一领域对象或领域服务负责的业务规则下沉到 Application。
- 领域层不得依赖 Spring、MyBatis、Jackson、HTTP 或数据库类型；依赖通过端口/接口反转。
- REST 输入输出必须使用明确的业务 DTO；普通业务接口不得使用 `JsonNode`、`Map`、`Object` 或裸 `byte[]` 表示业务数据。
- Raw Event 是保留未知/未支持输入的特殊用例，允许通过专门的原始证据类型保留完整事件；它不构成通用 JSON 接口，也不能替代已知事件的结构化模型。
- 聚合根、Entity、Value Object 和领域服务必须以业务概念命名，并注释其含义、职责、不变量和对外接口；仅在字段具有稳定业务行为时引入 Value Object，不为简单 ID 或时间值机械包装。
- 持久化端口统一命名为 `*Repository`，并按聚合根或独立生命周期实体拆分；不得以 `Store`、`DataService` 等技术中性名称掩盖领域职责。
- 一个 Repository 只负责一个聚合或独立实体的一致性边界。原始证据（如 `RawHookEvent`）、异步任务（如 `NormalizationJob`）和业务聚合（如 Session/Turn/Tool）即使同处一条流程，也必须使用不同 Repository；Application Use Case 负责组合它们并定义事务。
- 同一问题子域内、围绕任一单一聚合的“读取当前状态 → 应用领域规则 → 保存新状态”是通用的聚合状态演进模式，必须封装为该聚合的领域服务；Application 不得直接出现 `Repository.find → aggregate.apply → Repository.save` 三步。Application 只负责编排跨问题子域或多个领域服务的用例流程。领域服务可依赖定义在 Domain 的 Repository 端口，但不得依赖 Spring、MyBatis 或具体持久化实现。
- 注入或保存 Repository 的字段、构造参数和局部变量必须以完整 Repository 名称命名（如 `rawHookEventRepository`），不得以复数领域对象名（如 `rawHookEvents`）代替，以便从调用点直接识别持久化边界。
- Mapper 只负责简单查询、插入、更新和数据库约束配合；状态迁移、跨实体规则、关联等级、证据构建和业务计算必须在 Java 应用/领域层完成。
- 任何时候不得以牺牲可读性换取代码短小。方法、条件分支、构造参数和复杂表达式应按职责换行并保持可审阅；禁止将多步逻辑、`if`/`try-catch` 或多个语句压缩到单行。需要复用时优先提取有业务语义的私有方法，而不是堆叠嵌套或内联表达式。
- 类型、方法、字段和参数必须使用业务命令、业务事实或业务查询命名。业务代码不得使用脱离上下文后无法判断目的的 `save`、`get`、`process`、`handle` 等泛化名称；例如应使用 `linkTranscriptItemToTurn`，而不是用 `save` 隐藏绑定规则。
- Controller、Scheduler、Application Use Case、Domain Service/Policy 的类和公开业务方法必须注释业务目的、前置条件、核心规则、事务/一致性语义、结果和失败类别。复杂私有方法必须说明算法步骤与“不这样做会破坏什么规则”；简单访问器和自解释语句不增加复述代码的注释。
- 依赖外部目录结构、文件格式或协议字段的规则，必须集中记录在对应的版本化适配器或契约类型上，并引用协议版本或合成 fixture。领域代码不得散落没有来源、适用版本和降级方式的协议假设。

## 日志
- 日志记录到***~/.my_logs/{本项名称}***目录下，区分 frontend, backend, cli 目录存储
- 日志按日归档，最多存储7天
- 目前前端不写日志，只有后端和CLI才写日志
- 所有具备业务职责的公开入口都必须打印“调用入参摘要、返回值摘要、耗时、结果/异常类别”。业务公开入口包括 Controller HTTP 接口、Scheduler 调度入口、Application Use Case 公开方法和 Domain Service 的公开业务能力；不包括构造器、getter/setter、record accessor、Repository、Mapper 与实体内部状态方法。日志必须使用字段白名单、稳定 Hash 或原文摘要，禁止直接打印敏感入参、返回内容或异常原文。

## 必须读取的项目上下文

在规划或实施任何修改前，必须完整阅读 `prd_and_design/README.md`，并继续完整阅读其中“文档索引”引用的所有文档。将这些文件视为项目的持久上下文，不得依赖历史对话补充缺失的需求。

处理实现问题时，按以下优先级参考：

1. `prd_and_design/01-product-requirements.md` 定义产品语义、指标口径和数据边界。
2. `prd_and_design/prototype-v2` 定义当前已确认的 Hook-first 页面结构、视觉层级和交互方式；V1 仅用于追溯旧基线。
3. `prd_and_design/03-data-and-correlation.md` 定义 Hook、JSONL、OTel 和跨数据源关联规则。
4. `prd_and_design/02-technical-design.md` 定义工程实现方式。

如果这些来源互相冲突，或者无法确定一项会实质影响产品的决策，不得自行选择或静默假设。必须说明冲突，并先更新需求，再开始实现。

进入正式编码前必须执行需求、技术方案、原型和当前实现的一致性门禁：所有阻塞问题必须先解决；其他强烈建议解决的问题必须逐项明确提出并取得用户确认后，方可继续编码。

## 编译与运行环境要求

- java使用jdk17
- python使用3.11。使用uv做项目管理，项目级别的运行环境
- 数据库使用mysql

## 受保护的设计基线

- `prd_and_design/prototype-v1` 是已冻结、可复现的设计基线。不得修改、格式化或重新生成该目录中的任何文件。
- `prd_and_design/prototype-v2` 是当前已确认并冻结的设计基线。进入正式编码后不得修改、格式化或重新生成；新变化必须先更新 PRD 并创建 V3。
- 产品或交互发生变化时，必须先更新 PRD 和原型规格。需要新的可运行原型时，创建 `prd_and_design/prototype-v2` 等新版本，禁止覆盖 V1。
- 正式前端功能只能在 `frontend` 中实现。
- `backend` 目录及其里程碑建立后，后端功能只能在 `backend` 中实现。

## 事务与数据库逻辑

- 事务边界、提交与回滚统一由 Spring 的 `@Transactional` 或 `TransactionTemplate` 管理；业务流程、校验和重试优先在 Java 服务层实现。
- 尽量不使用 MySQL 触发器、存储过程、存储函数或数据库定时事件承载业务逻辑与事务编排。确有必要时，必须先在技术设计中说明 Java/Spring 方案不足、数据库权限要求及维护成本。
- 保留数据库事务、主外键、唯一约束和必要索引，作为持久化原子性、完整性与并发幂等的保障；不能仅用 Java 校验替代数据库约束。
- 回滚测试优先在 Java 测试边界注入异常，通过真实测试数据库验证写入和检查点一同回滚；不得为故障注入要求 MySQL 全局高权限或修改全局配置。

## 网页访问与浏览器自动化

- 搜索和发现候选网页时使用 hosted web search；搜索结果摘要只用于定位候选来源，不能作为目标页面已经加载或页面内容已经核验的证据。
- 打开、渲染、交互或验证具体本地/外部网页时统一使用 Playwright，由 Playwright 启动并控制独立浏览器页面；不得依赖 Safari、Chrome 等浏览器的既有标签页或其他浏览器控制通道。访问后必须至少校验最终 URL、HTTP 状态、页面标题或关键页面元素，确认目标页面已真实加载。

## 测试要求

- 前端和后端代码均必须编写并维护相应的单元测试。
- 单元测试必须能够在不依赖外部环境的情况下运行，不得要求网络、外部服务、真实 Codex 数据、本机专属配置或其他未由测试自身提供的运行条件。
- 后端接口可以在实施过程中独立测试，但这种阶段性测试不代表产品主流程或全部代码已经完成。
- 执行浏览器 E2E 测试前，必须先完成当前需求范围内的全部实现代码，并确认各交付面已经真实连通；不得使用 Mock 数据、占位实现或仅验证孤立页面交互来替代正式端到端链路。
- 当用户要求“完成所有代码”“实现完整代码”或同等含义的任务时，“所有代码”是指当前需求所需的全部对应实现，包括但不限于前端、后端，以及安装器、forwarder、Python CLI 或其他必要的命令行程序与脚本。不得把其中任一部分静默排除在完成范围之外。
- 宣布 E2E 或全部代码完成前，必须验证真实主流程能够跨越所有相关组件；若仍有未实现模块、Mock 数据源、未接通接口或缺失的 CLI/脚本，必须明确报告为未完成，不得缩小验收口径后宣称完成。

## 文档同步与仓库安全

- 已确认的需求、指标口径、架构、数据结构或关联规则发生变化时，必须同步更新 `prd_and_design`。
- 本项目是公开仓库。禁止提交真实账号信息、凭据、Token、私钥、本机专属路径、真实 Codex 对话或其他敏感数据。
- 示例、测试数据和截图必须使用明确的虚构内容，例如 `/workspace/demo-project` 和固定测试 ID。
- 禁止提交依赖目录、构建产物、本地数据库、JSONL 采集文件、日志或本地配置。
